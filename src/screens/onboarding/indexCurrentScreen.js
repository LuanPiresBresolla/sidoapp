const indexCurrentScreen = {
  currentScreen: 1,
  addIndex: function () {
    this.currentScreen++;
  },
  removeIndex: function () {
    this.currentScreen--;
  },
};

export default indexCurrentScreen;
